#schema './schema.canon'

use getEnv

PluginHeader {
    name = 'Plugin1'
    description = 'Description of plugin1'
    version = Version(0, 0, 1)
}

pipeline {
    build {
        command = 'go build'
    }
    test {
        script = 'go test ' + getEnv('PIPELINE_HOME_PATH') + '/test'
    }
}
