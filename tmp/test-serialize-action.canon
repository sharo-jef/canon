@serialize('toString')
struct Version {
    major: int = 1
    minor: int = 0
    patch: int = 0
    
    get toString(): string {
        "${major}.${minor}.${patch}"
    }
}

val v = Version()
val printAction: action = { -> }
