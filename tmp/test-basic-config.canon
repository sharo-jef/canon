#schema "./test-schema.canon"

// シンプルな設定ファイル
val name = "test-app"
val version = "1.0.0"
val debug = true
val port = 8080

// 設定構造体
config {
    appName = name
    appVersion = version
    isDebug = debug
    serverPort = port
}
