// Test if expression with blocks
struct TestStruct {
    value: int = 42
    result: string = if (value > 0) {
        "positive"
    } else {
        "negative"
    }
    
    // Test error() as regular function call
    validation() {
        if (value < 0) {
            error("Value cannot be negative")
        }
    }
}
